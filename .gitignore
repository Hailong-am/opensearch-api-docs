node_modules/
package-lock.json

# Local AWS probe / test scripts — never commit (may contain real resource IDs,
# endpoints, or account numbers). Keep these outside the repo or under these names.
*-probe.sh
*probe*.sh
scratch/
.local-test/

# build/ is entirely generated by tools/build-distribution-specs.sh and published
# to the gh-pages branch by the publish-docs CI workflow -- never commit it to main.
build/
bin/
